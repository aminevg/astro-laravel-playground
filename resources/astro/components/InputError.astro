---
interface Props extends astroHTML.JSX.HTMLAttributes {
    message?: string;
}

const { message, class: className, ...rest } = Astro.props;
---

<p {...rest} class:list={["text-sm text-red-600 dark:text-red-400", className]}>
    {message}
</p>
